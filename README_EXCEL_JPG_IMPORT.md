# Автоматическая подготовка датасета: Excel + JPG

В DeepCore выберите **File → Импорт Excel + JPG для обучения…**, затем:

1. выберите Excel с послойным седиментологическим описанием;
2. выберите папку с JPG-фото одного интервала отбора;
3. один раз выберите скважину и интервал из списка, прочитанного из Excel;
4. строки Excel сопоставятся по полям «№ скв.» и «Интервал фации по бурению: Кровля/Подошва»;
5. будут созданы контуры, коды фаций, краткое описание и автоматически извлечённые параметры.

На выходе создаётся новый проект и файл excel_jpg_import_report.txt. В нём перечислены проблемные фото. Нижняя подпись на JPG не используется и OCR не требуется.

## Как устроена привязка

Выбранный интервал из Excel задаёт диапазон всей пачки JPG. Интервалы фаций из Excel делятся между обнаруженными колонками керна в порядке чтения слева направо и сверху вниз. Каждая автоматически созданная граница остаётся редактируемой в приложении.

Перед массовым дообучением проверьте несколько десятков фото из разных скважин: совпадение скважины, верх/низ интервала и геометрию границ.

## Что запоминается при дообучении

Основная метка модели — код фации (если он заполнен; иначе индекс, затем название). Индекс, название, код, энергия среды и гидродинамический режим сохраняются в проекте и в справочнике `facies_catalog.json` рядом с созданной моделью `best.pt`. При распознавании этой моделью справочник автоматически возвращает эти поля в параметры выделенной фации. Для одной основной метки в Excel должны быть одинаковые индекс, название и код.

В карточке фации все описательные параметры независимы: в каждом выпадающем списке есть пункт «Самостоятельный выбор…» для своего значения. Колонка «Литология» на планшете и в Word/PDF-отчёте временно не показывается.
